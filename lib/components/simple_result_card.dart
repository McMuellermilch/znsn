import 'package:flutter/material.dart';

class SimpleResultCard extends StatelessWidget {
  SimpleResultCard({@required this.budget, @required this.compoundInterest});

  final int budget;
  final double compoundInterest;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
